{
  "name": "raycast-chatgpt-integration",
  "version": "0.1.0",
  "description": "Raycast custom provider for ChatGPT subscription-backed models",
  "private": true,
  "type": "module",
  "title": "ChatGPT Account Provider",
  "icon": "extension-icon.png",
  "author": "thomas",
  "platforms": [
    "macOS",
    "Windows"
  ],
  "categories": [
    "Productivity",
    "Developer Tools"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "sign-in",
      "title": "Sign in with ChatGPT",
      "description": "Authenticate your ChatGPT account for the local provider proxy.",
      "mode": "view"
    },
    {
      "name": "install-provider",
      "title": "Install Raycast AI Provider",
      "description": "Register the local ChatGPT provider in Raycast AI custom providers.",
      "mode": "view"
    },
    {
      "name": "proxy-status",
      "title": "ChatGPT Provider Status",
      "description": "Start, stop, and inspect the local provider proxy.",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "proxyPort",
      "type": "textfield",
      "title": "Proxy Port",
      "description": "Localhost port used by the OpenAI-compatible proxy.",
      "default": "18792",
      "required": true
    }
  ],
  "scripts": {
    "build": "pnpm build:daemon:asset && ray build",
    "build:daemon": "tsc -p tsconfig.daemon.json",
    "build:daemon:asset": "pnpm build:daemon && rm -rf assets/daemon-dist && mkdir -p assets/daemon-dist && pnpm exec esbuild src/daemon/server.ts --bundle --platform=node --format=cjs --target=node22 --outfile=assets/daemon-dist/server.cjs",
    "dev": "pnpm build:daemon:asset && ray develop",
    "lint": "ray lint",
    "test": "vitest run",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.daemon.json --noEmit"
  },
  "dependencies": {
    "@mariozechner/pi-ai": "^0.73.1",
    "@raycast/api": "^1.104.16",
    "@raycast/utils": "^2.2.4",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.1.1",
    "@types/node": "^22.19.17",
    "@types/react": "^19.0.10",
    "@vitest/coverage-v8": "^4.0.18",
    "esbuild": "^0.27.7",
    "react": "^19.2.6",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  },
  "packageManager": "pnpm@10.23.0"
}
